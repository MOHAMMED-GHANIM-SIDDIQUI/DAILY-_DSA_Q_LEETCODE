class Solution {
public:
    bool doesAliceWin(string s) {
        string vowels = "aeiou";
        int count = 0;
        for (char c : s) {
            if (vowels.find(c) != string::npos) {
                count++;
            }
        }
        return count > 0;
    }
};
